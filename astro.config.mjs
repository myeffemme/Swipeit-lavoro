// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // URL assoluti per canonical + condivisione social.
  site: 'https://www.myitalianjob.it',
});
